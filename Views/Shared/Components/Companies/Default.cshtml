@model Company_Managment.Models.CompaniesList

@{
    ViewData["Title"] = "My Companies";
}
My Companies
@foreach (var item in Model.CompanyList)
{
    if (item.former == 1)
    {
        <br />
        @Html.ActionLink(item.Name, "MCompany", "Managment", new { id = item.id }, null);
    }

}
<p>
    Working in
    @foreach (var item in Model.CompanyList)
    {
        if (item.former == 0)
        {
            <br />
            @Html.ActionLink(item.Name, "Info", "InfoCompany", new { idi = item.id }, null);
        }

    }
</p>
@*<table>
    <tr>
        <th>Company Name</th>
        <th>NumberOfWorkers</th>
        <th>Creation Date</th>
        <th>Creator</th>
    </tr>
       @foreach (var item in Model.CompanyList)
       {
        <tr>
            <td>@Html.DisplayFor(modelItem =>item.Name)</td>
            <td>@Html.DisplayFor(modelItem =>item.workers.Count)</td>
            <td>@Html.DisplayFor(modelItem =>item.CreateDate)</td>
            <td>@Html.DisplayFor(modelItem =>item.CreatorName)</td>
        </tr>
       }
    </table>*@

